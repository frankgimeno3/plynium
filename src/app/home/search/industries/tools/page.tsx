import React, { FC } from 'react';
import Skeleton from '../Skeleton';

interface ToolsProps {
  
}

const Tools: FC<ToolsProps> = ({ }) => {
  return (
    <Skeleton industryElement='construction tools'/>
  );
};

export default Tools;