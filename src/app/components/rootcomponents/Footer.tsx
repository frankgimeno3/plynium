import React, { FC } from 'react';

interface FooterProps {
  
}

const Footer: FC<FooterProps> = ({ }) => {
  return (
    <footer className='flex flex-row p-5 bg-black text-white'>
        Footer
    </footer>
  );
};

export default Footer;