import React, { FC } from 'react';
import Skeleton from '../Skeleton';

interface SoftwareProps {
  
}

const Software: FC<SoftwareProps> = ({ }) => {
  return (
    <Skeleton industryElement='software for construction'/>
  );
};

export default Software;