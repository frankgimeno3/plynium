import React, { FC } from 'react';

interface RequestsProps {
  
}

const Requests: FC<RequestsProps> = ({ }) => {
  return (
    <div>
        <p>Esta es para las requests de quotes, solo cuando ya tienes activado esto</p>
    </div>
  );
};

export default Requests;